version: "3.9"

services:
  web:
    image: nginx
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
      update_config:
        parallelism: 1
        delay: 5s
    ports:
      - "8080:80"
    networks:
      - webnet

networks:
  webnet:
    driver: overlay
